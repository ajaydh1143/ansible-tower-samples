---
- name: test play book
  host:
  gather_facts: false
  vars_files:
  
  
  tasks:
  - name: ansible password
    set_fact:
      ansible_user: "{{ username }}"
      ansible_password: "{{ password }}"
    no_log: true
  
  - name: set password
  
   
   
